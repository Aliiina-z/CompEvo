# 项目改动清单

## 📁 改动文件位置

**主要改动都在：** `/Users/bytedance/Desktop/llm_news/llm_egt_forecaster-master/llm_egt_forecaster/`

**项目根目录也有一些文件：** `/Users/bytedance/Desktop/llm_news/llm_egt_forecaster-master/`

---

## 🔧 核心代码改动（在 llm_egt_forecaster/ 目录下）

### 1. 项目结构重组

**改动：** 将 `configs/`、`data/`、`src/` 移动到 `llm_egt_forecaster/` 目录下

**原因：** 修复包导入问题，使 `from llm_egt_forecaster.configs import base_config` 能正常工作

**创建的文件：**
- `llm_egt_forecaster/__init__.py` - 包初始化文件
- `llm_egt_forecaster/data/__init__.py` - 数据包初始化文件

---

### 2. 配置文件修改

**文件：** `llm_egt_forecaster/configs/base_config.py`

**改动内容：**
1. **添加 MPS_LLAMA 模式支持**
   ```python
   # 添加了 MPS_LLAMA 模式（Apple Silicon GPU）
   elif MODE == 'MPS_LLAMA':
       DEVICE = "mps"
       BASE_LLM_MODEL = "meta-llama/Llama-3.1-8B"
       USE_QUANTIZATION = False
       BATCH_SIZE = 1
   ```

2. **自动模式检测**
   ```python
   # 自动检测 MPS，如果可用则使用 MPS_LLAMA，否则使用 CPU_DEBUG
   MODE = 'MPS_LLAMA' if (hasattr(torch.backends, 'mps') and torch.backends.mps.is_available()) else 'CPU_DEBUG'
   # 当前临时设置为 CPU_DEBUG
   MODE = 'CPU_DEBUG'
   ```

---

### 3. 数据生成器修改

**文件：** `llm_egt_forecaster/data/data_generator.py`

**改动内容：**
1. **添加格式检查，避免重复转换**
   ```python
   # 检查是否已经是转换后的格式
   if len(raw_dataset) > 0:
       first_sample = raw_dataset[0]
       if "time_series" in first_sample and "candidate_news" in first_sample:
           print("Data is already in converted format. Skipping conversion.")
           return raw_dataset
   ```

2. **改进错误处理**
   - 保持原有的转换逻辑
   - 跳过格式不正确的样本，而不是全部失败

---

### 4. 数据集修改（重要修复）

**文件：** `llm_egt_forecaster/src/dataset.py`

**改动内容：**
1. **添加自定义 collate 函数**（修复 RuntimeError）
   ```python
   def custom_collate_fn(batch):
       """
       处理变长的 candidate_news 列表
       """
       time_series_strs = [item["time_series_str"] for item in batch]
       candidate_news_list = [item["candidate_news"] for item in batch]  # 保持为列表
       ground_truths = torch.stack([item["ground_truth"] for item in batch])
       return {
           "time_series_str": time_series_strs,
           "candidate_news": candidate_news_list,
           "ground_truth": ground_truths,
       }
   ```

2. **所有 DataLoader 使用自定义 collate**
   ```python
   DataLoader(..., collate_fn=custom_collate_fn)
   ```

**原因：** 修复 `RuntimeError: each element in list of batch should be of equal size` 错误

---

### 5. 进化框架修改

**文件：** `llm_egt_forecaster/src/models/evolutionary_framework.py`

**改动内容：**
1. **修复换行符问题**
   - 清理了 Windows 换行符（\r）导致的语法错误

2. **简化 _prepare_batch 方法**
   ```python
   def _prepare_batch(self, batch):
       # 简化逻辑，因为 candidate_news 现在已经是正确格式
       ground_truths = batch["ground_truth"].to(self.device)
       time_series_strs = batch["time_series_str"]
       candidate_news_batch = batch["candidate_news"]  # 已经是正确格式
       return time_series_strs, candidate_news_batch, ground_truths
   ```

3. **添加类型导入**
   ```python
   from typing import List, Tuple, Dict
   ```

---

### 6. 新闻选择器修改

**文件：** `llm_egt_forecaster/src/models/news_selector.py`

**改动内容：**
1. **使 openai 成为可选依赖**
   ```python
   try:
       import openai
       OPENAI_AVAILABLE = True
   except ImportError:
       OPENAI_AVAILABLE = False
       logger.warning("OpenAI package not installed. API mode will not be available.")
   ```

2. **添加 API 模式检查**
   ```python
   if not OPENAI_AVAILABLE:
       raise ImportError("OpenAI package is required for API mode...")
   ```

**原因：** 避免在没有安装 openai 时导入错误（当前使用 cosine 模式，不需要 openai）

---

### 7. 训练入口文件修改

**文件：** `train.py`（项目根目录）

**改动内容：**
1. **更新数据文件路径**
   ```python
   data_filepath = "llm_egt_forecaster/data/real_dataset.json"
   ```

---

## 📄 新增文档和脚本（项目根目录）

### 文档文件：
1. **运行指南.md** - 从零开始的运行指南
2. **GPU和运行环境说明.md** - GPU 配置和内存使用说明（已删除）
3. **项目执行流程详解.md** - 详细的执行流程说明
4. **错误诊断和解决方案.md** - 常见错误和解决方案
5. **模式切换说明.md** - 模式切换指南
6. **服务器部署指南.md** - 服务器部署步骤
7. **项目改动清单.md** - 本文件

### 脚本文件：
1. **切换模式.sh** - 交互式模式切换脚本
2. **恢复MPS模式.sh** - 一键恢复 MPS_LLAMA 模式

---

## 📊 改动统计

### 修改的文件（在 llm_egt_forecaster/ 目录下）：
1. ✅ `llm_egt_forecaster/configs/base_config.py` - 添加 MPS 支持，模式配置
2. ✅ `llm_egt_forecaster/data/data_generator.py` - 添加格式检查
3. ✅ `llm_egt_forecaster/src/dataset.py` - **重要**：添加自定义 collate 函数
4. ✅ `llm_egt_forecaster/src/models/evolutionary_framework.py` - 修复语法错误，简化 batch 处理
5. ✅ `llm_egt_forecaster/src/models/news_selector.py` - 使 openai 可选

### 创建的文件：
1. ✅ `llm_egt_forecaster/__init__.py` - 包初始化
2. ✅ `llm_egt_forecaster/data/__init__.py` - 数据包初始化

### 项目根目录修改：
1. ✅ `train.py` - 更新数据路径

---

## 🎯 关键修复

### 1. 修复 RuntimeError（最重要）
- **问题：** `RuntimeError: each element in list of batch should be of equal size`
- **原因：** candidate_news 是变长列表，PyTorch 默认 collate 无法处理
- **解决：** 添加 `custom_collate_fn` 函数

### 2. 修复语法错误
- **问题：** `SyntaxError: unexpected character after line continuation character`
- **原因：** Windows 换行符（\r）导致
- **解决：** 清理换行符

### 3. 修复数据转换问题
- **问题：** 重复转换导致所有样本失败
- **解决：** 添加格式检查，避免重复转换

### 4. 修复导入错误
- **问题：** `ModuleNotFoundError: No module named 'openai'`
- **解决：** 使 openai 成为可选依赖

---

## 📝 总结

**所有核心代码改动都在：**
```
/Users/bytedance/Desktop/llm_news/llm_egt_forecaster-master/llm_egt_forecaster/
```

**主要改动：**
- ✅ 5 个 Python 文件被修改
- ✅ 2 个 __init__.py 文件被创建
- ✅ 1 个根目录文件被修改（train.py）
- ✅ 多个文档和脚本被创建（项目根目录）

**关键修复：**
1. 数据 collate 问题（最重要）
2. 语法错误
3. 数据转换问题
4. 导入错误

所有改动都是为了：
- 修复代码错误
- 支持 macOS（MPS）
- 优化内存使用
- 提高代码健壮性

