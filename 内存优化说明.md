# 内存优化说明

## 🎯 已应用的内存优化（针对CPU_DEBUG模式）

为了降低内存占用以便跑通模型，我已经对配置进行了以下优化：

### 主要优化项

1. **代理数量**: `10 → 3`
   - 内存节省: 约 70% (每个代理需要独立 LoRA 参数)
   - 影响: 策略多样性降低，但核心功能保留

2. **批次大小**: `4 → 1`
   - 内存节省: 约 75% (批次大小直接影响内存峰值)
   - 影响: 训练速度可能稍慢，但内存占用大幅降低

3. **LoRA Rank**: `8 → 4`
   - 内存节省: 约 50% (每个代理的 LoRA 参数减半)
   - 影响: 模型容量降低，但对跑通流程影响不大

4. **序列长度**: `1024 → 512`
   - 内存节省: 约 50% (注意力机制的内存是序列长度的平方)
   - 影响: 可能无法处理超长序列，但对于大多数情况足够

5. **训练轮次**: `3 → 1`
   - 时间节省: 约 67%
   - 影响: 仅用于验证流程，不适合完整训练

6. **新闻选择数量**: `3 → 2`
   - 内存节省: 少量
   - 影响: 每个代理选择的新闻更少

## 📊 预期内存占用

### 优化前（原始配置）
- 基础模型 (distilgpt2): ~250MB
- 10个代理的LoRA: ~50MB × 10 = 500MB
- 批次大小为4的激活值: ~200MB
- 其他: ~100MB
- **总计**: ~1050MB

### 优化后（当前配置）
- 基础模型 (distilgpt2): ~250MB
- 3个代理的LoRA: ~25MB × 3 = 75MB (rank减半)
- 批次大小为1的激活值: ~50MB (序列长度减半)
- 其他: ~50MB
- **总计**: ~425MB

**内存节省约 60%**

## ⚙️ 如何进一步优化（如果需要）

如果仍然内存不足，可以进一步调整以下参数：

### 1. 进一步减少代理数量

在 `base_config.py` 中修改：
```python
NUM_AGENTS = 2  # 甚至可以是 2 个代理
```

### 2. 进一步减小 LoRA Rank

```python
LORA_RANK = 2  # 最小可设为 2
LORA_ALPHA = 4  # 相应调整
```

### 3. 进一步减小序列长度

```python
MAX_LENGTH = 256  # 如果数据允许
```

### 4. 使用梯度累积（如果批次大小为1仍然太大）

虽然当前批次大小已经是1，但如果还是内存不足，可以：
- 减小 MAX_LENGTH
- 减少 NUM_AGENTS 到 1-2 个

## ⚠️ 注意事项

1. **这些优化仅用于跑通流程**，不适合完整训练
2. **代理数量减少**会降低模型的多样性，但核心的进化机制仍然保留
3. **批次大小为1**会降低训练稳定性，但不会影响流程验证
4. **训练轮次为1**仅用于快速验证，完整训练需要更多轮次

## 🔄 恢复到原始配置

如果需要恢复原始配置（用于完整训练），请修改 `base_config.py`：

```python
# CPU_DEBUG 模式
MAX_LENGTH = 1024
LORA_RANK = 8
LORA_ALPHA = 16
BATCH_SIZE = 4
NUM_EPOCHS = 3

# 通用配置
NUM_AGENTS = 10
NEWS_COSINE_TOP_K = 3
```

## 💡 建议

1. **首次运行**: 使用当前优化配置确保流程能够跑通
2. **流程验证后**: 如果内存允许，可以逐步增加参数
3. **完整训练**: 需要恢复原始配置或在更好的硬件上运行

## 🚀 运行

优化后的配置会自动应用，直接运行：
```bash
python train.py
```

系统会自动使用优化后的内存配置。

