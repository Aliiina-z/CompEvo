# LLM EGT Forecaster 从零开始运行指南

## ✅ 已完成的步骤

### 1. 环境准备
- ✅ Python 3.9.6 已安装
- ✅ 虚拟环境已创建在 `venv/` 目录
- ✅ 所有依赖包已安装

### 2. 项目配置
- ✅ 项目已配置为 `CPU_DEBUG` 模式（适合macOS，无CUDA支持）
- ✅ 数据文件已准备：`llm_egt_forecaster/data/real_dataset.json`
- ✅ 项目包结构已修复并安装

### 3. 项目结构
```
llm_egt_forecaster-master/
├── llm_egt_forecaster/      # 主包目录
│   ├── configs/            # 配置文件
│   ├── data/               # 数据文件
│   └── src/                # 源代码
├── train.py                # 训练入口脚本
├── venv/                   # 虚拟环境
└── requirements.txt        # 依赖列表
```

## 🚀 运行项目

### 激活虚拟环境并运行训练

```bash
cd /Users/bytedance/Desktop/llm_news/llm_egt_forecaster-master
source venv/bin/activate
python train.py
```

### 配置说明

当前配置（`llm_egt_forecaster/configs/base_config.py`）：
- **MODE**: `CPU_DEBUG` - 使用CPU模式，适合macOS
- **BASE_LLM_MODEL**: `distilgpt2` - 轻量级模型，适合调试
- **NUM_AGENTS**: 10 - 代理数量
- **NUM_EPOCHS**: 3 - 训练轮数
- **NEWS_SELECTOR_METHOD**: `cosine` - 使用本地embedding进行新闻筛选（无需API）

### 数据格式

数据文件位于：`llm_egt_forecaster/data/real_dataset.json`

数据格式要求：
```json
{
  "time_series": [1.2, 1.5, 1.8, ...],
  "candidate_news": [
    {
      "summary": "新闻摘要...",
      "publication_time": "2019-01-01 09:00:00",
      "category": "News"
    }
  ],
  "ground_truth": [2.1, 2.3, 2.5, ...]
}
```

## 📝 注意事项

1. **CPU模式性能**：CPU_DEBUG模式使用较小的模型（distilgpt2），训练速度较慢，主要用于验证逻辑
2. **GPU模式**：如果有NVIDIA GPU且支持CUDA，可以修改`configs/base_config.py`中的`MODE = 'GPU_LLAMA'`来使用更强大的模型
3. **数据转换**：数据文件会自动从instruction/input/output格式转换为标准格式
4. **训练时间**：CPU模式下训练可能需要较长时间，请耐心等待

## 🔧 常见问题

### 问题1：ModuleNotFoundError
**解决方案**：确保已激活虚拟环境并安装了项目包
```bash
source venv/bin/activate
pip install -e .
```

### 问题2：数据文件未找到
**解决方案**：确保数据文件在正确位置
```bash
ls llm_egt_forecaster/data/real_dataset.json
```

### 问题3：内存不足
**解决方案**：在`configs/base_config.py`中减小`BATCH_SIZE`或`NUM_AGENTS`

## 📊 训练输出

训练过程中会显示：
- 配置信息
- 数据加载进度
- 每个epoch的训练和验证指标（Loss, MSE, RMSE, MAE）
- 最终代理的逻辑和适应度

## 🎯 下一步

训练完成后，可以：
1. 查看训练日志和指标
2. 调整超参数（学习率、批次大小等）
3. 尝试不同的新闻筛选方法（cosine vs api）
4. 如果有GPU，切换到GPU_LLAMA模式获得更好性能

