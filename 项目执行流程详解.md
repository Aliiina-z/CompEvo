# 项目执行流程详解

本文档详细说明运行 `python train.py` 时，项目会用到哪些文件，经过哪些步骤。

## 📁 涉及的核心文件

### 1. 入口文件
- **`train.py`** - 主入口脚本

### 2. 配置文件
- **`llm_egt_forecaster/configs/base_config.py`** - 所有超参数和配置

### 3. 数据处理文件
- **`llm_egt_forecaster/data/data_generator.py`** - 数据加载和格式转换
- **`llm_egt_forecaster/src/dataset.py`** - PyTorch Dataset 和 DataLoader

### 4. 模型文件
- **`llm_egt_forecaster/src/models/evolutionary_framework.py`** - 主框架（多代理系统）
- **`llm_egt_forecaster/src/models/logic_generator.py`** - 逻辑生成器（进化逻辑）
- **`llm_egt_forecaster/src/models/news_selector.py`** - 新闻选择器（筛选相关新闻）
- **`llm_egt_forecaster/src/agent.py`** - 单个代理定义

### 5. 训练相关文件
- **`llm_egt_forecaster/src/engine/trainer.py`** - 训练器（训练循环）
- **`llm_egt_forecaster/src/engine/loss.py`** - 损失函数（进化损失）

### 6. 数据文件
- **`llm_egt_forecaster/data/real_dataset.json`** - 训练数据

---

## 🔄 完整执行流程

### 阶段 1: 初始化和配置加载

**步骤 1.1: 导入模块**
```python
# train.py 第 8-15 行
from llm_egt_forecaster.configs import base_config
from llm_egt_forecaster.data.data_generator import DataGenerator
from llm_egt_forecaster.src.dataset import get_dataloader
from llm_egt_forecaster.src.models.evolutionary_framework import EvolutionaryFramework
from llm_egt_forecaster.src.engine.loss import EvolutionaryLoss
from llm_egt_forecaster.src.models.logic_generator import LogicGenerator
from llm_egt_forecaster.src.engine.trainer import Trainer
```

**涉及文件:**
- `llm_egt_forecaster/configs/base_config.py` - 加载配置
- 所有被导入的模块文件

**步骤 1.2: 设置随机种子**
```python
# train.py 第 32-33 行
config = base_config
set_seed(config.SEED)  # 设置随机种子为 42
```

**作用:** 确保结果可复现

**步骤 1.3: 打印配置信息**
```python
# train.py 第 35-41 行
print("--- Configuration ---")
print(f"Device: {config.DEVICE}")  # mps/cpu/cuda
print(f"Base LLM: {config.BASE_LLM_MODEL}")  # meta-llama/Llama-3.1-8B
print(f"Number of Agents: {config.NUM_AGENTS}")  # 10
print(f"Number of Epochs: {config.NUM_EPOCHS}")  # 3
print(f"News Selector Method: {config.NEWS_SELECTOR_METHOD}")  # cosine
```

---

### 阶段 2: 数据准备

**步骤 2.1: 检查数据文件**
```python
# train.py 第 45-51 行
data_filepath = "llm_egt_forecaster/data/real_dataset.json"
if not os.path.exists(data_filepath):
    raise FileNotFoundError(...)
```

**涉及文件:**
- `llm_egt_forecaster/data/real_dataset.json`

**步骤 2.2: 加载和转换数据**
```python
# train.py 第 54-56 行
real_generator = DataGenerator(config, filepath=data_filepath)
real_generator.load_dataset()
```

**涉及文件:**
- `llm_egt_forecaster/data/data_generator.py`

**DataGenerator.load_dataset() 做了什么:**
1. 读取 JSON 文件
2. 将 instruction/input/output 格式转换为标准格式：
   ```json
   {
     "time_series": [1.2, 1.5, ...],
     "candidate_news": [
       {"summary": "...", "publication_time": "...", "category": "..."}
     ],
     "ground_truth": [2.1, 2.3, ...]
   }
   ```
3. 覆盖写回原文件（转换后的格式）

**步骤 2.3: 初始化 Tokenizer**
```python
# train.py 第 58-60 行
tokenizer = AutoTokenizer.from_pretrained(config.TOKENIZER_PATH)
if tokenizer.pad_token is None:
    tokenizer.pad_token = tokenizer.eos_token
```

**作用:** 
- 从 Hugging Face 下载 tokenizer（如果未缓存）
- 设置 padding token

**步骤 2.4: 创建 DataLoader**
```python
# train.py 第 63-81 行
train_dataloader = get_dataloader(...)  # 80% 训练集
val_dataloader = get_dataloader(...)   # 20% 验证集
```

**涉及文件:**
- `llm_egt_forecaster/src/dataset.py`

**get_dataloader() 做了什么:**
1. 创建 `NewsTimeSeriesDataset` 实例
2. 按 8:2 比例分割训练集和验证集
3. 创建 PyTorch DataLoader，返回：
   - `time_series_str`: 时间序列字符串
   - `candidate_news`: 候选新闻列表
   - `ground_truth`: 真实值张量

---

### 阶段 3: 模型初始化

**步骤 3.1: 初始化主框架**
```python
# train.py 第 90 行
framework = EvolutionaryFramework(config).to(config.DEVICE)
```

**涉及文件:**
- `llm_egt_forecaster/src/models/evolutionary_framework.py`

**EvolutionaryFramework.__init__() 做了什么:**
1. **加载基础 LLM 模型**
   - 从 Hugging Face 下载模型（如果未缓存）
   - 如果 `USE_QUANTIZATION=True`，使用 4-bit 量化
   - 移动到指定设备（mps/cpu/cuda）

2. **初始化 Tokenizer**
   - 加载 tokenizer
   - 设置 padding token

3. **创建预测头**
   - `nn.Linear(hidden_size, FUTURE_STEPS)` - 将 LLM 输出映射到预测值

4. **初始化代理群体**
   - 创建 `NUM_AGENTS`（默认 10）个 Agent
   - 每个 Agent 有：
     - 自己的 LoRA 适配器（个性化模型）
     - 一个可学习的 gate 参数
     - 初始逻辑（策略描述）

5. **初始化新闻选择器**
   - 根据 `NEWS_SELECTOR_METHOD` 选择方法：
     - `cosine`: 使用 SentenceTransformer 计算相似度
     - `api`: 使用 OpenAI API（需要安装 openai 包）

**涉及文件:**
- `llm_egt_forecaster/src/agent.py` - Agent 类定义
- `llm_egt_forecaster/src/models/news_selector.py` - 新闻选择器

**步骤 3.2: 初始化损失函数**
```python
# train.py 第 93 行
loss_fn = EvolutionaryLoss(config)
```

**涉及文件:**
- `llm_egt_forecaster/src/engine/loss.py`

**EvolutionaryLoss 包含:**
- `L_strategy`: 策略损失（预测误差）
- `L_diversity`: 多样性损失（鼓励代理策略多样化）
- `L_pruning`: 剪枝损失（惩罚表现差的代理）
- `L_pg`: 策略梯度损失（如果使用）

**步骤 3.3: 初始化逻辑生成器**
```python
# train.py 第 96 行
logic_generator = LogicGenerator(framework.base_model, tokenizer, config)
```

**涉及文件:**
- `llm_egt_forecaster/src/models/logic_generator.py`

**作用:** 用于生成和进化代理的策略逻辑（文本描述）

---

### 阶段 4: 训练器初始化

**步骤 4.1: 创建训练器**
```python
# train.py 第 100-107 行
trainer = Trainer(
    framework=framework,
    loss_fn=loss_fn,
    logic_generator=logic_generator,
    dataloader=train_dataloader,
    val_dataloader=val_dataloader,
    config=config
)
```

**涉及文件:**
- `llm_egt_forecaster/src/engine/trainer.py`

**Trainer.__init__() 做了什么:**
1. 保存所有组件引用
2. 创建优化器（AdamW）：
   ```python
   self.optimizer = AdamW(
       self.framework.parameters(),  # 所有可训练参数
       lr=config.LEARNING_RATE,      # 1e-4
       weight_decay=config.WEIGHT_DECAY  # 0.01
   )
   ```
3. 保存代理列表引用

---

### 阶段 5: 训练循环

**步骤 5.1: 开始训练**
```python
# train.py 第 110 行
trainer.train()
```

**涉及文件:**
- `llm_egt_forecaster/src/engine/trainer.py`

**trainer.train() 执行流程:**

#### 5.1.1 外层循环：Epoch 循环
```python
for epoch in range(NUM_EPOCHS):  # 默认 3 个 epoch
    # 训练一个 epoch
    train_epoch(epoch)
    
    # 验证
    validate()
```

#### 5.1.2 内层循环：Batch 循环（train_epoch）

对每个 batch：

**步骤 A: 生成新逻辑（策略进化）**
```python
# trainer.py 第 95-100 行
for agent in self.agents:
    new_logic, log_prob = self.logic_generator.generate(
        agent, self.agents, with_log_prob=True
    )
    agent.update_logic(new_logic)  # 临时更新逻辑
```

**涉及文件:**
- `llm_egt_forecaster/src/models/logic_generator.py`

**作用:** 
- 使用 LLM 为每个代理生成新的策略逻辑
- 例如："关注天气、经济活动和能源政策相关的新闻"

**步骤 B: 前向传播（评估新逻辑）**
```python
# trainer.py 第 106 行
framework_output = self.framework(batch)
```

**涉及文件:**
- `llm_egt_forecaster/src/models/evolutionary_framework.py`

**EvolutionaryFramework.forward() 做了什么:**

1. **准备批次数据**
   - 提取时间序列字符串
   - 提取候选新闻列表
   - 提取真实值

2. **对每个代理:**
   ```python
   for agent in self.agents:
       # a. 使用新闻选择器筛选相关新闻
       selected_news = self.news_selector.select(
           agent.logic,  # 代理的策略逻辑
           candidate_news_batch[i]  # 候选新闻
       )
       
       # b. 构建提示词
       prompt = PROMPT_TEMPLATE.format(
           future_steps=FUTURE_STEPS,
           time_series=time_series_str,
           selected_news=selected_news
       )
       
       # c. Tokenize
       inputs = tokenizer(prompt, ...)
       
       # d. 通过代理的 LoRA 模型前向传播
       outputs = agent(**inputs, output_hidden_states=True)
       
       # e. 提取最后一个隐藏状态
       last_hidden = outputs.hidden_states[-1][:, -1, :]
       
       # f. 通过预测头得到预测值
       prediction = self.prediction_head(last_hidden)
       
       # g. 计算奖励（负 MSE）
       reward = -F.mse_loss(prediction, ground_truth)
   ```

3. **聚合所有代理的预测**
   ```python
   # 根据代理的适应度和 gate 值计算权重
   weights = softmax(gates * fitnesses / temperature)
   
   # 加权平均得到最终预测
   aggregated_prediction = sum(weights * predictions)
   ```

4. **返回结果**
   ```python
   return {
       "aggregated_prediction": aggregated_prediction,
       "agent_predictions": [所有代理的预测],
       "agent_rewards": [所有代理的奖励],
       "agent_gates": [所有代理的 gate 值]
   }
   ```

**步骤 C: 计算优势（Advantage）**
```python
# trainer.py 第 112-114 行
baseline = rewards_new_logic.mean()
advantages = rewards_new_logic - baseline
```

**作用:** 
- 优势 = 代理奖励 - 平均奖励
- 用于策略梯度更新

**步骤 D: 计算损失**
```python
# trainer.py 第 125 行
loss_dict = self.loss_fn(
    framework_output, 
    ground_truth, 
    self.agents, 
    pg_components
)
```

**涉及文件:**
- `llm_egt_forecaster/src/engine/loss.py`

**EvolutionaryLoss 计算:**
```python
L_total = L_strategy + λ_diversity * L_diversity + λ_pruning * L_pruning + L_pg

其中:
- L_strategy: MSE(aggregated_prediction, ground_truth)
- L_diversity: 鼓励代理策略多样化
- L_pruning: 惩罚表现差的代理
- L_pg: 策略梯度损失 = -advantages * log_probs
```

**步骤 E: 反向传播和优化**
```python
# trainer.py 第 129-131 行
self.optimizer.zero_grad()
loss.backward()
self.optimizer.step()
```

**作用:**
- 更新所有可训练参数：
  - 代理的 LoRA 权重
  - 代理的 gate 参数
  - 预测头权重

**步骤 F: 更新代理状态**
```python
# trainer.py 第 134 行
self._update_agents_after_batch(framework_output["agent_rewards"])
```

**作用:**
- 使用指数移动平均更新代理的适应度：
  ```python
  agent.fitness = β * old_fitness + (1-β) * new_reward
  ```

**步骤 G: 更新进度条**
```python
# trainer.py 第 145-149 行
progress_bar.set_postfix({
    'loss': f'{loss.item():.4f}',
    'l_pg': f'{loss_dict["l_pg"]:.4f}',
    'adv_mean': f'{advantages.mean().item():.3f}'
})
```

#### 5.1.3 验证阶段（validate）

每个 epoch 结束后：

```python
# trainer.py 第 37-79 行
def validate(self):
    self.framework.eval()  # 设置为评估模式
    
    for batch in val_dataloader:
        # 前向传播（不更新参数）
        framework_output = self.framework(batch)
        
        # 计算损失和指标
        loss = loss_fn(...)
        mse = MSE(predictions, ground_truth)
        mae = MAE(predictions, ground_truth)
        rmse = sqrt(mse)
    
    # 打印验证结果
    print(f"Val Loss: {avg_val_loss:.4f}")
    print(f"Val MSE: {avg_mse:.4f}")
    print(f"Val RMSE: {avg_rmse:.4f}")
    print(f"Val MAE: {avg_mae:.4f}")
```

---

### 阶段 6: 训练完成

**步骤 6.1: 打印最终结果**
```python
# train.py 第 112-115 行
print("\n--- Evolution Process Complete ---")
print("Final Agent Logics and Fitness:")
for agent in trainer.agents:
    print(f"- Agent {agent.id}: Fitness={agent.fitness:.4f}, Logic='{agent.logic}'")
```

**输出示例:**
```
--- Evolution Process Complete ---
Final Agent Logics and Fitness:
- Agent 0: Fitness=0.8234, Logic='关注天气和能源政策相关的新闻...'
- Agent 1: Fitness=0.7891, Logic='分析经济活动和基础设施事件...'
...
```

---

## 📊 数据流向图

```
数据文件 (real_dataset.json)
    ↓
DataGenerator.load_dataset()
    ↓ (格式转换)
标准格式 JSON
    ↓
NewsTimeSeriesDataset
    ↓ (8:2 分割)
DataLoader (train/val)
    ↓
Batch: {time_series_str, candidate_news, ground_truth}
    ↓
EvolutionaryFramework.forward()
    ↓
    ├─→ NewsSelector.select() → 筛选相关新闻
    ├─→ Agent (LoRA模型) → 生成预测
    └─→ 聚合所有代理预测
    ↓
framework_output: {predictions, rewards, gates}
    ↓
EvolutionaryLoss()
    ↓
loss_dict: {total_loss, l_strategy, l_diversity, ...}
    ↓
loss.backward() + optimizer.step()
    ↓
更新参数 (LoRA权重, gates, 预测头)
```

---

## 🔑 关键概念

### 1. 代理（Agent）
- 每个代理是一个独立的预测模型
- 有自己的 LoRA 适配器（个性化）
- 有自己的策略逻辑（文本描述）
- 有自己的适应度分数

### 2. 进化过程
- **逻辑生成**: 使用 LLM 生成新的策略逻辑
- **评估**: 用新逻辑进行预测，计算奖励
- **选择**: 根据适应度选择好的策略
- **更新**: 通过梯度下降更新模型参数

### 3. 新闻选择
- **Cosine 方法**: 使用 embedding 计算相似度
- **API 方法**: 使用 GPT 智能选择（需要 API key）

### 4. 损失函数
- **策略损失**: 预测准确性
- **多样性损失**: 鼓励策略多样化
- **剪枝损失**: 淘汰表现差的代理
- **策略梯度损失**: 强化学习更新

---

## ⚠️ 注意事项

1. **首次运行**: 会从 Hugging Face 下载模型和 tokenizer（需要网络）
2. **内存占用**: MPS_LLAMA 模式需要较大内存（建议 16GB+）
3. **训练时间**: 每个 epoch 可能需要数小时（取决于数据量和硬件）
4. **Hugging Face 认证**: 使用 Llama 模型需要先登录并申请访问权限

---

## 🎯 总结

运行 `python train.py` 的完整流程：

1. **加载配置** → 2. **准备数据** → 3. **初始化模型** → 4. **初始化训练器** → 5. **训练循环** → 6. **输出结果**

整个过程涉及约 **10 个核心文件**，实现了：
- 多代理竞争进化
- 新闻驱动的时序预测
- 策略自动优化
- 端到端训练

