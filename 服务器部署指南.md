# 服务器部署指南

## 📋 当前项目状态

✅ **已完成的工作：**
- 项目环境配置完成
- 数据文件准备就绪（3655个样本）
- 代码错误已修复（collate函数、数据转换等）
- 支持 CPU_DEBUG 和 MPS_LLAMA 模式

⚠️ **本地运行问题：**
- 内存不足（Killed: 9）
- 10个代理 + 模型 + 数据占用内存过大

## 🚀 服务器部署步骤

### 1. 上传项目到服务器

```bash
# 在本地打包项目（排除虚拟环境和缓存）
cd /Users/bytedance/Desktop/llm_news
tar -czf llm_egt_forecaster-master.tar.gz \
    --exclude='llm_egt_forecaster-master/venv' \
    --exclude='llm_egt_forecaster-master/__pycache__' \
    --exclude='llm_egt_forecaster-master/.git' \
    --exclude='llm_egt_forecaster-master/checkpoints'
    llm_egt_forecaster-master/

# 上传到服务器
scp llm_egt_forecaster-master.tar.gz user@server:/path/to/destination/
```

### 2. 在服务器上解压和设置

```bash
# SSH 登录服务器
ssh user@server

# 解压项目
cd /path/to/destination/
tar -xzf llm_egt_forecaster-master.tar.gz
cd llm_egt_forecaster-master

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install --upgrade pip
pip install -r requirements.txt
pip install -e .
```

### 3. 配置项目模式

根据服务器配置选择模式：

#### 方案 A: 有 NVIDIA GPU（推荐）

编辑 `llm_egt_forecaster/configs/base_config.py`：

```python
MODE = 'GPU_LLAMA'  # 使用 CUDA GPU
```

**要求：**
- NVIDIA GPU（>=16GB VRAM 推荐）
- CUDA 支持
- 需要 Hugging Face 认证（Llama 模型）

#### 方案 B: 只有 CPU 但内存充足

编辑 `llm_egt_forecaster/configs/base_config.py`：

```python
MODE = 'CPU_DEBUG'  # CPU 模式
```

**优化配置（减少内存占用）：**
```python
NUM_AGENTS = 5  # 从 10 减少到 5
BATCH_SIZE = 2  # 从 4 减少到 2（如果内存仍然不足）
```

### 4. 准备数据文件

确保数据文件在正确位置：

```bash
# 检查数据文件
ls -lh llm_egt_forecaster/data/real_dataset.json

# 如果不存在，需要上传
scp /path/to/local/real_dataset.json user@server:/path/to/llm_egt_forecaster-master/llm_egt_forecaster/data/
```

### 5. 运行训练

```bash
# 激活虚拟环境
source venv/bin/activate

# 如果使用 GPU_LLAMA 模式，先登录 Hugging Face
huggingface-cli login

# 运行训练
python train.py

# 或者使用 nohup 在后台运行
nohup python train.py > train.log 2>&1 &

# 查看日志
tail -f train.log
```

## ⚙️ 服务器配置建议

### GPU 服务器（推荐）

**推荐配置：**
- GPU: NVIDIA A100 / V100 / RTX 3090 / RTX 4090
- VRAM: >= 16GB
- RAM: >= 32GB
- 存储: >= 100GB（用于模型缓存）

**配置：**
```python
MODE = 'GPU_LLAMA'
NUM_AGENTS = 10  # 可以使用更多代理
BATCH_SIZE = 2   # GPU 模式默认值
```

### CPU 服务器（备选）

**推荐配置：**
- CPU: 多核（>= 8 cores）
- RAM: >= 32GB（推荐 64GB）
- 存储: >= 50GB

**配置：**
```python
MODE = 'CPU_DEBUG'
NUM_AGENTS = 5   # 减少代理数量
BATCH_SIZE = 2   # 减小批次大小
```

## 🔧 内存优化建议

如果服务器内存仍然紧张，可以：

### 1. 减少代理数量

```python
# configs/base_config.py
NUM_AGENTS = 3  # 从 10 减少到 3
```

### 2. 减小批次大小

```python
# CPU_DEBUG 模式
BATCH_SIZE = 1  # 从 4 减少到 1
```

### 3. 减少训练数据

```python
# train.py 中修改 train_ratio
train_ratio=0.5  # 只用 50% 数据训练
```

### 4. 使用梯度累积

如果批次太小，可以使用梯度累积来模拟更大的批次。

## 📊 监控训练

### 使用 screen 或 tmux

```bash
# 使用 screen
screen -S training
python train.py
# 按 Ctrl+A 然后 D 退出（进程继续运行）
# 重新连接: screen -r training

# 使用 tmux
tmux new -s training
python train.py
# 按 Ctrl+B 然后 D 退出
# 重新连接: tmux attach -t training
```

### 查看资源使用

```bash
# GPU 使用情况（如果有 GPU）
nvidia-smi

# CPU 和内存使用
htop
# 或
top
```

## 📝 检查清单

上传到服务器前，确认：

- [ ] 项目代码已上传
- [ ] 数据文件已上传
- [ ] 服务器有足够的存储空间（模型缓存）
- [ ] Python 版本 >= 3.8
- [ ] 已安装 CUDA（如果使用 GPU）
- [ ] 已配置 Hugging Face 认证（如果使用 Llama 模型）
- [ ] 虚拟环境已创建并安装依赖

## 🎯 快速启动命令

```bash
# 1. 上传项目（本地）
tar -czf llm_egt_forecaster.tar.gz --exclude='venv' --exclude='__pycache__' llm_egt_forecaster-master/
scp llm_egt_forecaster.tar.gz user@server:/path/

# 2. 服务器设置（SSH 到服务器）
cd /path/
tar -xzf llm_egt_forecaster.tar.gz
cd llm_egt_forecaster-master
python3.9 -m venv venv
source venv/bin/activate

conda create -n test python=3.9 \
  -c https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main
pip install -r requirements.txt
pip install -e .

# 3. 配置模式（编辑配置文件）
vim llm_egt_forecaster/configs/base_config.py
# 设置 MODE = 'GPU_LLAMA' 或 'CPU_DEBUG'

# 4. 运行训练
huggingface-cli login  # 如果需要

cd llm_egt_forecaster-master
python -m venv venv
source venv/bin/activate

export HF_ENDPOINT="https://hf-mirror.com" HF_HUB_DOWNLOAD_TIMEOUT="300" HF_HUB_ENABLE_HF_TRANSFER="0"
cd llm_egt_forecaster
nohup python3 /HOME/sysu_dfli/sysu_dfli_1/yuxuan/llm_egt_forecaster-master-12-24/llm_egt_forecaster/train.py \
  > /HOME/sysu_dfli/sysu_dfli_1/yuxuan/llm_egt_forecaster-master-12-24/llm_egt_forecaster/log/train_1227_mae_batch=4_pe_maxtoken.out \
  2>&1 &
python train.py
```

## 💡 提示

1. **首次运行**：会下载模型，需要稳定的网络连接
2. **模型缓存**：模型会缓存在 `~/.cache/huggingface/`，避免重复下载
3. **训练时间**：GPU 模式每个 epoch 可能需要 30 分钟到数小时
4. **日志保存**：建议使用 `nohup` 或 `screen` 保存训练日志

祝您在服务器上训练顺利！🚀

