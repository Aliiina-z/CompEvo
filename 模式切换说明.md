# 模式切换说明

## ✅ 当前状态

**当前模式**: `CPU_DEBUG` ✅

已切换到 CPU_DEBUG 模式，可以立即运行测试，无需 Hugging Face 认证。

## 🚀 现在可以运行

```bash
cd /Users/bytedance/Desktop/llm_news/llm_egt_forecaster-master
source venv/bin/activate
python train.py
```

## 🔄 恢复 MPS_LLAMA 模式

当您准备好使用 MPS_LLAMA 模式时，有两种方式恢复：

### 方式 1: 使用恢复脚本（推荐）

```bash
cd /Users/bytedance/Desktop/llm_news/llm_egt_forecaster-master
./恢复MPS模式.sh
```

### 方式 2: 手动修改配置文件

编辑 `llm_egt_forecaster/configs/base_config.py`，将第 12 行改为：

```python
MODE = 'MPS_LLAMA' if (hasattr(torch.backends, 'mps') and torch.backends.mps.is_available()) else 'CPU_DEBUG'
```

## ⚠️ 使用 MPS_LLAMA 模式前的准备

在恢复 MPS_LLAMA 模式之前，请确保：

1. **申请 Llama 模型访问权限**
   - 访问: https://huggingface.co/meta-llama/Llama-3.1-8B
   - 点击 "Agree and access repository"
   - 填写申请表单

2. **登录 Hugging Face**
   ```bash
   huggingface-cli login
   ```
   - 输入您的 token（在 https://huggingface.co/settings/tokens 获取）

3. **恢复模式**
   ```bash
   ./恢复MPS模式.sh
   ```

## 📊 两种模式对比

| 特性 | CPU_DEBUG | MPS_LLAMA |
|------|-----------|-----------|
| 模型 | distilgpt2 (500MB) | Llama-3.1-8B (16GB) |
| 认证 | ❌ 不需要 | ✅ 需要 |
| 性能 | 较低（调试用） | 高 |
| 内存 | 2-4GB | 20-25GB |
| 速度 | 慢 | 快（GPU加速） |
| 适用场景 | 代码调试、快速测试 | 正式训练 |

## 📝 关于数据转换警告

如果看到类似这样的警告：
```
[Warning] Failed to convert sample 3538: 'instruction'
```

这是正常的，说明：
- 数据转换器会跳过格式不正确的样本
- 只要成功转换的样本数量足够（通常 > 1000 个），就可以正常训练
- 这些警告不影响训练过程

## 🎯 快速命令

```bash
# 查看当前模式
grep "^MODE = " llm_egt_forecaster/configs/base_config.py

# 切换到 CPU_DEBUG
./切换模式.sh  # 选择选项 1

# 切换到 MPS_LLAMA
./恢复MPS模式.sh
```

